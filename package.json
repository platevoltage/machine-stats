{
  "name": "my-electron-app",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsc && cp -R src/public dist/ && npm run build:react && electron dist",
    "start:dev": "tsc && cp -R src/public dist/ && electron dist",
    "build:react": "cd src/react/popup && npm i && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^21.2.2",
    "electron-is-dev": "^2.0.0"
  }
}
